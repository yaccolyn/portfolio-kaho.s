<%= link_to favorite_path(current_user.favorites.find_by(post_id: post.id)), id: "unfavorite-button-for-post-#{post.id}", class: "link-blue", data: { turbo_method: :delete } do %>
  <i class="bi bi-map-fill h4"></i>
<% end %>